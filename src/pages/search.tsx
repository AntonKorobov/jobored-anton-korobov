import styles from '@/styles/pages/Search.module.scss';

import React from 'react';

import Layout from '@/components/layout';

export default function Search() {
  return (
    <Layout>
      <div className={styles.searchPage}>Search</div>
    </Layout>
  );
}
